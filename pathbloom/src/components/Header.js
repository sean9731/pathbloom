import React from 'react';
import './Header.css'; // Add the styles for the header

const Header = () => {
  return (
    <header className="header">
      <h1 className="header-title">PathBloom</h1>
    </header>
  );
};

export default Header;